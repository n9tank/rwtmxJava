package rust.tmx;

public class objective extends basic {
 public objective(float x0, float y0, triggers triggers) {
  super(x0, y0, 0f, 0f, triggers);
 }
 public objective(float x0, float y0, point g) {
  super(x0, y0, 0f, 0f, g);
 }
}
